# Core -- everything the placement algorithm and its CLI need
numpy>=1.24
scipy>=1.11
tqdm>=4.66
pyyaml>=6.0

# Ray tracing, evaluation, plotting and the notebooks
matplotlib>=3.8
plotly>=5.18
kaleido>=0.2          # plotly static image export
imageio>=2.33
trimesh>=4.0
scikit-image>=0.22
pandas>=2.1

# Tests
pytest>=7.0

# Sionna RT is installed separately -- follow the official instructions for
# your CUDA version:  pip install sionna
